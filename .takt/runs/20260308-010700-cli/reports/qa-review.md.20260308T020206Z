# QAレビュー

## 結果: REJECT

## サマリー
今回変更で追加された `runFacetCli` の新規エラーパスに対するテストが不足しています。`new` 指摘が1件あるため REJECT です。

## 確認した観点
| 観点 | 結果 | 備考 |
|------|------|------|
| テストカバレッジ | ❌ | `runFacetCli` の新規異常系3パスが未検証 |
| テスト品質 | ✅ | 既存追加テスト（統合・単体）は明確で再現性あり |
| エラーハンドリング | ✅ | 実装は Fail Fast で明示的エラーを返す |
| ドキュメント | ✅ | CLI利用方法と compose 定義の説明が追加済み |
| 保守性 | ✅ | 責務分離（CLI/定義読込/出力）が維持されている |

## 今回の指摘（new）
| # | finding_id | family_tag | カテゴリ | 場所 | 問題 | 修正案 |
|---|------------|------------|---------|------|------|--------|
| 1 | QAREV-001 | test-coverage | テスト | `src/cli/index.ts:153`, `src/cli/index.ts:168`, `src/cli/index.ts:172`, `src/__tests__/it-cli-compose-flow.test.ts:30` | 新規挙動（`Unsupported command` / `No compose definitions found` / `Unknown compose definition selected`）に対する回帰テストがない。 | `it-cli-compose-flow.test.ts` に3ケース（不正コマンド、定義0件、候補外選択）を追加して失敗系を検証する。 |

## 継続指摘（persists）
| # | finding_id | family_tag | 前回根拠 | 今回根拠 | 問題 | 修正案 |
|---|------------|------------|----------|----------|------|--------|
| なし | - | - | - | - | - | - |

## 解消済み（resolved）
| finding_id | 解消根拠 |
|------------|----------|
| TREV-001 | `src/__tests__/it-cli-compose-flow.test.ts:95` で `knowledge < policy` を直接検証し、`src/__tests__/compose.test.ts:78` で `userMessageOrder` 単体検証を追加済み。 |
| TREV-002 | `src/__tests__/init.test.ts:38` および `src/__tests__/init.test.ts:71` でテンプレート初回生成と再実行時非上書きを検証済み。 |

## 再開指摘（reopened）
| # | finding_id | family_tag | 解消根拠（前回） | 再発根拠 | 問題 | 修正案 |
|---|------------|------------|----------------|---------|------|--------|
| なし | - | - | - | - | - | - |

## REJECT判定条件
- `new`、`persists`、または `reopened` が1件以上ある場合のみ REJECT 可
- `finding_id` なしの指摘は無効