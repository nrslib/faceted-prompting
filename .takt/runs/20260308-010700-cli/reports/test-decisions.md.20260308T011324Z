# 決定ログ

## 1. `facet compose` に対してインテグレーションテストを追加する
- **背景**: 初期化（init）→定義解決（resolve）→合成（compose）→出力（output）を跨ぐ新規データフローが追加予定で、3つ以上のモジュール横断に該当するため。
- **検討した選択肢**: ユニットテストのみで分割して検証する / 主要フローを1本の結合テストで検証する
- **理由**: モジュール単体では検出できない「選択値と出力先の伝搬」を担保する必要があるため、結合テストを追加した。

## 2. compose定義ディレクトリ名の期待値を `compositions` に固定する
- **背景**: 計画レポートで compose 定義ディレクトリの候補があり、テスト側で期待値を確定する必要があった。
- **検討した選択肢**: `composes` / `compositions`
- **理由**: 計画レポート内の確認事項で `compositions` が最終候補として示されており、テスト期待値として一貫したため。
